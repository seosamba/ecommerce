<?php $id = uniqid('store-coupon-'); ?>
<div>
    <h3 class="checkout-widget-title">
	    <?php echo $this->translate('Enter coupon');?>
	    <?php if (count(Tools_ShoppingCart::getInstance()->getCoupons()) > 0): ?>
        <a href="<?php echo $this->websiteUrl . $this->returnUrl;?>?step=shipping" class="checkout-edit" title="<?php echo $this->translate('Edit coupons');?>">[&nbsp;<?php echo $this->translate('edit');?>&nbsp;]</a>
	    <?php endif; ?>
    </h3>
    <form id="<?php echo $id; ?>" class="store-coupon" action="<?php echo trim($this->websiteUrl, '/').$_SERVER['REQUEST_URI']; ?>" method="post">
	    <input type="text" name="code" required>
	    <input type="submit" value="<?php echo $this->translate('Apply'); ?>">
	</form>
	<?php if (isset($this->errors)): ?>
	<ul class="errors">
		<?php foreach($this->errors as $err): ?>
			<li><?php echo $this->translate($err); ?></li>
		<?php endforeach; ?>
	</ul>
	<?php endif; ?>
</div>
<script>
$(function(){
	$('#<?php echo $id; ?>').on('submit', function(e){
        e.preventDefault();

        var self = this;

		var codeInput = $(this).find("input[name=code]");
		if (codeInput.val() == ''){
			codeInput.focus();
			return false;
		}

		$.ajax({
			url: $('#website_url').val()+'plugin/shopping/run/coupon/',
			data: $(this).serialize(),
			type: 'POST',
			dataType: 'json',
			success: function(r){
				console.log(r);
				if (!!r.error){
					showMessage(r.responseText, true);
				} else {
					if (typeof  r.responseText === 'object'){
						$.each(r.responseText, function(key, msg) {
							$("<p></p>", { "class": "store-coupon-notify" }).html(msg)
									.on('click', function(){ $(this).remove(); })
									.insertAfter(self);
						});
					}
				}
				$(self).trigger('reset');
                refreshCartSummary();
			}
		})
	});
});
</script>