import merge from 'lodash/merge';

let defaultState = {
};

let state = {};
merge(state, defaultState);

const actions = {

};

const mutations = {
}

const getters = {
};
export default {
    state,
    actions,
    getters,
    mutations
};